def format_customer_info(customer):

    # Format and return customer info

    return f"{customer.name}, {customer.address}"


def format_supplier_info(supplier):

    # Format and return supplier info

    return f"{supplier.name}, {supplier.address}"
